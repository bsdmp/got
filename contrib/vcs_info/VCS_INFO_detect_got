## vim:ft=zsh
## got support
## Distributed under the same BSD-ish license as zsh itself.

setopt localoptions NO_shwordsplit

[[ $1 == '--flavours' ]] && return 1

vcs_comm[detect_need_file]=repository
VCS_INFO_bydir_detect '.got'
return $?
